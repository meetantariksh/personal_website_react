// import { fromJS } from 'immutable';
// import { selectTwiterFeedContainerDomain } from '../selectors';

describe('selectTwiterFeedContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
